import OverviewSection from './OverviewSection'

// Phase 1 — renders OverviewSection only.
// New Home UI will be built in a later phase.
export default function HomeTab(props) {
  return <OverviewSection {...props} />
}
